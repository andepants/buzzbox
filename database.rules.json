{
  "rules": {
    // ============================================
    // DEVELOPMENT-ONLY WIDE OPEN RULES
    // WARNING: These rules are COMPLETELY INSECURE and allow ALL access
    // ONLY use for development/testing - NEVER in production!
    // TODO: Replace with production rules before launch
    // ============================================
    ".read": true,
    ".write": true
  }
}
