# ================================================
# Epic 6: AI-Powered Creator Inbox
# Environment Variables for Firebase Cloud Functions
# ================================================

# ================================================
# OpenAI Configuration
# ================================================
# Get your API key from: https://platform.openai.com/api-keys
# Click "Create new secret key" and copy it here
# IMPORTANT: Never commit the actual .env file to git!
OPENAI_API_KEY=sk-proj-your-key-here

# ================================================
# Firebase Project Configuration
# ================================================
# Find your project ID in Firebase Console > Project Settings
# Should match your Firebase project (e.g., buzzbox-prod)
FIREBASE_PROJECT_ID=your-project-id

# ================================================
# Local Development Notes
# ================================================
# 1. Copy this file to .env (without .example)
# 2. Replace placeholder values with your actual credentials
# 3. Make sure .env is in .gitignore (it should be by default)
# 4. For production deployment, use Firebase Secrets:
#    firebase functions:secrets:set OPENAI_API_KEY

# ================================================
# Production Deployment
# ================================================
# DO NOT use .env for production
# Instead, use Firebase Secrets:
#
# Set secret:
#   firebase functions:secrets:set OPENAI_API_KEY
#
# Access in code:
#   process.env.OPENAI_API_KEY
#
# List secrets:
#   firebase functions:secrets:access OPENAI_API_KEY
